package com.example.databaseexample;

// ContactModel class represents a single contact item
public class ContactModel {
    int id;           // Unique identifier for the contact (primary key in the database)
    String name;      // Name of the contact
    String phone_no;  // Phone number of the contact
}